{"default":{"identifier":"default","description":"The webview talks to the core and nothing else: no filesystem plugin, no shell. The dialog is for one thing — choosing a folder to scan — and a volume is chosen from the list the core reports, not through a file picker.","local":true,"windows":["main"],"permissions":["core:default","dialog:allow-open"]}}